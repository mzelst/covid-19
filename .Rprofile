require(base)
require(utils)
require(stats)
require(rmarkdown)
require(cowplot)
require(tidyverse)
require(rjson)
require(rtweet)
require(data.table)
require(git2r)
require(lubridate)
require(rvest)
require(jsonlite)
require(R.utils)
require(magick)
require(webshot2)
require(anytime)
require(cbsodataR)
require(sf)
require(extrafont)
git.credentials <- read_lines("git_auth.txt")
git.auth <- cred_user_pass(git.credentials[1],git.credentials[2])
repo <- init()








